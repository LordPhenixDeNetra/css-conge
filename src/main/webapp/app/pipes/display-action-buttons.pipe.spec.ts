import { DisplayActionButtonsPipe } from './display-action-buttons.pipe';

describe('DisplayActionButtonsPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayActionButtonsPipe();
    expect(pipe).toBeTruthy();
  });
});
